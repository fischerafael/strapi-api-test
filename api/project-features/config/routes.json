{
  "routes": [
    {
      "method": "GET",
      "path": "/project-features",
      "handler": "project-features.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-features/count",
      "handler": "project-features.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-features/:id",
      "handler": "project-features.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-features",
      "handler": "project-features.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-features/:id",
      "handler": "project-features.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-features/:id",
      "handler": "project-features.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
